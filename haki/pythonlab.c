#include <stdio.h>
int main(){
    printf("Hello\n");
    int a,b;
    scanf("%d%d",&a,&b);
    //format specifiers are used to search the value refered to that specified data type
    printf("%f %f",a,b);
    return 0;
}