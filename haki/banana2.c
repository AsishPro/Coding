#include <stdio.h>
int main(){
    int k,n,w;
    scanf("%d%d%d",&k,&n,&w);
    int sum=0;
    sum=((w*(w+1))/2)*k;
    if(sum>n)
    printf("%d",sum=sum-n);
    else
    printf("0");


    //or for loop
}